{{ form_start(form, {'attr': {'class': 'space-y-4', 'enctype': 'multipart/form-data'}}) }}
    <div class="space-y-4">
        {{ form_widget(form, {'attr': {'class': 'block w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500'}}) }}
    </div>
    <button class="bg-indigo-500 text-white px-4 py-2 rounded hover:bg-indigo-700">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
