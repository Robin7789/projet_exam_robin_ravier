{% extends 'base.html.twig' %}

{% block title %}CollectionGame{% endblock %}

{% block body %}
    <h1>CollectionGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_game.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_game.name }}</td>
            </tr>
            <tr>
                <th>PersonalNote</th>
                <td>{{ collection_game.personalNote }}</td>
            </tr>
            <tr>
                <th>AddedDate</th>
                <td>{{ collection_game.addedDate ? collection_game.addedDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collection_game_index') }}">back to list</a>

    <a href="{{ path('app_collection_game_edit', {'id': collection_game.id}) }}">edit</a>

    {{ include('collection_game/_delete_form.html.twig') }}
{% endblock %}
